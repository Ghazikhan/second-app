<br>
<% if user_signed_in? %>
	<div class="list-group">
		<span class="list-group-item active">
			<h4><b>Categories</b></h4>
		</span>
		<% @advertise.jobs.each do |job| %>
		<span class="list-group-item">
		<span class="badge">04</span>
			<%= job.degre.name_with_type %>
		</span>
		<% end %>
	</div>
<% end %>
